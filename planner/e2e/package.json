{
  "name": "aegira-planner-e2e",
  "private": true,
  "description": "Playwright-E2E/Persona-Tests (Teststrategie §9/§10, P1). Isoliert vom App-Build (eigene Deps → kein Einfluss auf npm ci des Frontends).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "install:browsers": "playwright install --with-deps chromium webkit",
    "lh:a11y": "node lighthouse/a11y.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.1",
    "@playwright/test": "1.60.0",
    "lighthouse": "^13.3.0",
    "puppeteer": "^25.1.0"
  }
}
