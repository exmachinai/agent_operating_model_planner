---
name: show-plan
description: Zeigt den Plan aus `$HARNESS_ROOT/plan/`.
disable-model-invocation: true
---

# /show-plan

Zeigt den Plan aus `$HARNESS_ROOT/plan/`.

Arbeitet ausschließlich auf absoluten Pfaden ab `$HARNESS_ROOT`.
